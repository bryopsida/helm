name: Release Chart
on:
  push:
    branches:
      - main
    paths:
      - "charts/**"
      - ".github/workflows/chart-release.yml"
jobs:
  Release:
    name: 'Helm'
    uses: curium-rocks/flows/.github/workflows/helm-release.yml@main
